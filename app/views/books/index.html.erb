<h1>Books List</h1>

<% if @books.any? %>
  <ul>
    <% @books.each do |book| %>
      <li><%= link_to book.title, book_path(book) %></li>
    <% end %>
  </ul>
<% else %>
  <p>No books available.</p>
<% end %>

<%= link_to 'Add a new book', new_book_path %>
